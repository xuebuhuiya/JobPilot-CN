# 2026-09-16 实现与扩大测试记录

## 已实现

- `jobpilot.py`：统一 SQLite 岗位池、只读 BossHunter 导入、手工 JSON 来源导入、独立资格/JD质量检查、Markdown 报告。
- `review`：人工结论与原因；`application`：仅记录申请状态与证据，不触发提交。
- 原始评分和判断依据完整保留；导入去重、历史快照、事件审计；岗位/评分/候选资格变化后撤销旧审阅结论，保留既有申请状态。
- `tests/test_jobpilot.py`：13 项测试全部通过，包括真实 CLI 的人工记录、重复导入与事务回滚。

使用说明见 [统一岗位池](job-pool.md)。当前为命令行与报告入口，没有修改 BossHunter 前端，也没有把规则判断写回它的 ready 状态。因此应以本地 `data/job-pool.md` 为统一审查结果，不把上游按钮上的“待确认”视为规则已通过。

## 原有 9 条回归结果

| 新规则状态 | 数量 |
|---|---:|
| 明确资格不符，排除 | 1 |
| JD 信息不足 | 1 |
| 资格或年限待核实 | 6 |
| 可进入人工评估 | 1 |

原始高分不再覆盖资格/内容问题；规则是第一版启发式，不代表人工核对已完成。所有真实岗位、简历信息和事实依据都留在被 Git 忽略的本地目录。

## 校招硬筛选更新（v2.1）

按社招目标，明确校招/应届/在校标签或正文身份限定直接排除，原始技术评分不覆盖结果。招聘类型未知、相互矛盾或只有上游推断分类时保留待核实。检查发现上游会用宽泛关键词推断 campus，因此不将该字段单独作为硬排除证据。

19 项测试通过。原有9条重新评估：排除4条、JD信息不足1条、资格待核实3条、可人工评估1条。施耐德电气与九方智投原有高分记录已因在校/应届标签排除。本次只重算已采集数据，没有新增采集或投递；BossHunter网页原始状态未改写。

## 扩大测试

目标：在原9条基础上新增最多21条，总量约30条；AI应用工程师最多11条、Agent开发最多10条，各最多2页；先做资格/质量检查，再对可评分的新增记录调用模型。

实际执行两次有限尝试：

1. 专用 Chrome 与运行时断开，首词两页均未成功解析，新记录0，任务停止。诊断确认 disconnected 后重启原专用 Chrome 配置。
2. 连接恢复后，首次搜索返回 `captcha`，任务立即停止，新记录0，未尝试第二词、未新增模型评分、未投递。

前两次未完成扩大测试；随后本人重新登录 Chrome，并授权继续，结果如下。重启浏览器未修改账号凭据、未取消平台风控或保护策略。

本地证据：`data/expanded-20260916-140532/summary.json`、`data/expanded-20260916-140811/summary.json`；每次均保存测试前数据库快照。原始小样本证据仍位于 `data/smoke-20260916-000225/`。

### 重新登录后的完成结果

- 新增21条：AI应用工程师11条、Agent开发10条；共3个搜索页，解析/保存失败均为0，达到数量上限后正常停止。
- DeepSeek成功评分19条，2条因JD内容不足跳过。新增结果：可人工评估4条、资格待核实14条、低匹配1条、JD信息不足2条。
- 共30条历史记录，其中4条在本轮开始前已由用户移入回收站，保持移除，有效记录26条。全库状态：可人工评估5条、待核实15条、低匹配1条、信息不足3条、硬排除2条、来源已移除4条。
- 已核对新增ID、19条评分结果、2条跳过项及运行前后history一致性；没有打招呼或投递。高分前十名（第十名并列，共11条）已对照JD核查。
- 本批没有明确校招限定的新岗位，新增硬排除为0；校招硬筛选证据来自旧样本回归和此前19项测试，不能以本批无校招样本宣称检出率。

发现的问题：上游校招分类缺少原文依据；列表城市与标题实际地点可能冲突；模型可能把总工作年限等同于专项LLM年限；同公司相近岗位不同ID可能重发。当前保留待核实和原始证据，尚未自动纠正这些字段。

本地证据：`data/expanded-20260916-144954/summary.json`、`validation.json`、`before.db`、`测试报告.md`；报告及真实岗位数据不提交Git。

## 尚未实现

- 自有 LLM 评分算法（目前仍保留 BossHunter 原评分）；完整英文/复杂语义资格识别。
- 跨来源自动合并、CSV 导出、网页审阅入口及浏览器自动填表。
- 简历自动定制与统一材料版本审核。

下一步优先核对新增4条可人工评估岗位，以及技术相关度高但招聘类型待核实的岗位；完善分类证据、地点冲突检测和专项经验年限后，再准备定制简历。
